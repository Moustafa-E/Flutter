class Quote {
  String? text;
  String? author;

  Quote({this.text, this.author});
  // shorthand form for constructor. Sets this class's field to whatever is passed into it during instantiation. 
}
